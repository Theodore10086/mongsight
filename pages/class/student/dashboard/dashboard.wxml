<view class="page class-flow {{layoutClass}}">
  <view class="profile">
    <view class="profile-card">
      <view class="profile-paper" aria-hidden="true"></view>
      <text class="profile-welcome">欢迎同学：{{studentName}}</text>
      <text class="profile-class">当前班级：{{className}}</text>
    </view>
  </view>

  <view class="section-head">
    <view class="section-gold" aria-hidden="true"></view>
    <text class="section-title">我的作业</text>
  </view>

  <block wx:if="{{assignmentList.length > 0}}">
    <view
      class="card"
      wx:for="{{assignmentList}}"
      wx:key="id"
      data-id="{{item.id}}"
      data-status="{{item.status}}"
      bindtap="goToAssignment"
    >
      <view class="card-main">
        <view class="card-left">
          <text class="card-title">{{item.title}}</text>
          <text class="card-date">布置时间：{{item.date}}</text>
        </view>
        <view class="card-right">
          <block wx:if="{{item.status === 'pending'}}">
            <view class="card-row-status">
              <text class="tag-undone">未交</text>
            </view>
            <view class="btn btn-practice" hover-class="btn--hover">去练习</view>
          </block>
          <block wx:elif="{{item.status === 'inprogress'}}">
            <view class="card-row-status">
              <text class="tag-progress">{{item.progressText}}</text>
            </view>
            <view class="btn btn-practice" hover-class="btn--hover">继续练习</view>
          </block>
          <block wx:else>
            <view class="card-row-status">
              <text class="tag-done">已完成</text>
            </view>
            <view class="btn btn-outline btn-disabled">等待批阅</view>
          </block>
        </view>
      </view>
    </view>
  </block>

  <view wx:else class="empty-wrap">
    <text class="empty-text">老师暂时还没布置作业哦～</text>
  </view>
</view>
